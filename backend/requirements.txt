# ── Core API ──────────────────────────────────────────────────────────────────
# All versions pinned to have pre-built cp311 wheels — no Rust/C source builds
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Auth / Security ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 4.x broke passlib 1.7.4's verify() in certain code paths.
# Pin to last fully-compatible version.
bcrypt==4.0.1
# cryptography pinned to a version with cp311 manylinux wheel
cryptography==42.0.8

# ── Pydantic / Settings ───────────────────────────────────────────────────────
pydantic[email]==2.7.1
pydantic-settings==2.2.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# ── Task Queue ────────────────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.4

# ── Scraping ──────────────────────────────────────────────────────────────────
scrapy==2.11.2
itemadapter==0.9.0
w3lib==2.2.1
beautifulsoup4==4.12.3
lxml==5.2.2

# ── Image handling ────────────────────────────────────────────────────────────
Pillow==10.4.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx==0.27.0
requests==2.31.0

# ── Search / NLP ──────────────────────────────────────────────────────────────
rapidfuzz==3.9.3
unidecode==1.3.8

# ── Data / Export ─────────────────────────────────────────────────────────────
# numpy before pandas — required for pandas metadata resolution
numpy==1.26.4
pandas==2.2.2
openpyxl==3.1.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
aiofiles==23.2.1
loguru==0.7.2
tenacity==8.3.0
